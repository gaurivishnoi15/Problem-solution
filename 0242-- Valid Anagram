class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        str = []
        tar = []
        str.extend(s)
        tar.extend(t)
        str.sort()
        tar.sort()
        if str == tar:
            return True
        else:
            return False
