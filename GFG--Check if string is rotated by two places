
class Solution:
    #Function to check if a string can be obtained by rotating
    #another string by exactly 2 places.
    def isRotated(self,str1,str2):
        if ((str1[2:] + str1[:2] == str2) or (str2[2:] + str2[:2] == str1)):
            return 1
        return 0
        #code here
